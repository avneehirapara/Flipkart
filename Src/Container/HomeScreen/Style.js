import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        margin: 16,
        flex: 1,
        padding: 5,
    },
    backIcon: {
        position:'absolute',
        left:0,
        fontSize: 30,
        color: 'black',
      },
 
  
});

export default styles